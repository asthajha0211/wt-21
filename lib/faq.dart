class MyItem {
  MyItem(
      {this.isExpanded: false, this.header, this.body, this.hex, this.bodyhex});

  bool isExpanded;
  final String header;
  final String body;
  final hex;
  final bodyhex;
}
